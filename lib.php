<?php declare(strict_types = 1);

// function createFolder( string $value,  string $directory) {
//     $names =  scandir($directory);
//     if (!in_array($value, $names )){
//         mkdir($directory . $value);
//     };
// }